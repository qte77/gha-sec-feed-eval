### Added

- Repository scaffold per the
  [breezy-greeting-engelbart plan](.claude/plans/breezy-greeting-engelbart.md):
  `pyproject.toml` (hatchling + bumpversion + scriv + ruff with TRY + pyright
  + complexipy ≤10 + `[tool.uv].exclude-newer`), `Makefile` (KISS qte77
  conventions adapted from `scrape-stock-kpi`), AGENTS / CONTRIBUTING /
  AGENT_LEARNINGS / AGENT_REQUESTS governance docs, `.markdownlint.json`,
  `.lychee.toml`, `.gitmessage`, devcontainer for Python 3.12, Claude Code
  settings enabling `python-dev` + `gha-dev` + `tdd-core` + `commit-helper`
  + `security-audit` from `qte77-claude-code-utils`, scriv-managed
  CHANGELOG with `<!-- scriv-insert-here -->` marker, default categories
  config at `categories/default.yaml`, architecture / contracts / scoring
  / categories docs, stub package skeleton at `src/gha_sec_feed_eval/`
  (real `cli.py` + `models.py` + `scoring.py` etc. land in phase 2b).

- CI baseline: `validate.yaml` (ruff + pyright + complexipy + markdownlint
  + pytest+cov), `codeql.yaml` (Python with inline-comment dismissal),
  `links-fail-fast.yaml` (lycheeverse/lychee-action), `sbom.yaml`
  (qte77/gha-sbom-action), `actionlint.yaml`, `trivy.yaml`,
  `osv-scanner.yaml`, `gitleaks.yaml`, and a 3-workflow release split
  `bump-my-version.yaml` → `tag-release.yaml` → `publish-release.yaml`
  (signed tag objects via GitHub API per gh-security-posture rule `03`,
  Syft SBOM + `actions/attest-build-provenance` on the source tarball at
  publish). Every workflow uses workflow-scope `permissions: {}` with
  narrowest per-job grants and `step-security/harden-runner` as first
  step (egress-policy `audit`; tightens to `block` in 2d).

### Security

- All `uses:` SHA-pinned to full-length commit hashes. New action SHAs
  resolved fresh at scaffold time:
  `step-security/harden-runner@9af89fc7…` (v2.19.4),
  `reviewdog/action-actionlint@6fb7acc9…` (v1.72.0),
  `aquasecurity/trivy-action@ed142fd0…` (v0.36.0),
  `google/osv-scanner-action@9a498708…` (v2.3.8),
  `gitleaks/gitleaks-action@e0c47f4f…` (v3.0.0),
  `actions/attest-build-provenance@a2bbfa25…` (v4.1.0),
  `anchore/sbom-action@e22c3899…` (v0.24.0).
- `tag-release.yaml` produces server-signed tag objects via
  `POST /repos/.../git/tags` rather than `git tag -a -m`, satisfying
  gh-security-posture rule `03` (`required_signatures` on `refs/tags/v*`).
